'b'+'a'++'a'+'a';
baNaNa = ++'a'를 not a num 약자로 NaN가 되어서 ba NaN a

console.log 변수 확인용(=php var_dump)

일반적으로 js 불러올때는 body 최하단에서 불러옴
>html 태그 사용 시 위에서 불러올 시에는 그 태그에 적용할 수 없음

js 위에서 부터 순서대로 실행
js는 오류 라인 있을 시 하위코드 실행 하지 않음

함수레벨 스코프
>함수 내 변수로 설정한 값은 함수로써 호출해야함
>외부에서 변수명으로 호출 불가

블록레벨 스코프
>{} 한 블록 내에서 변수로 설정한 값은 블록 내에서 console.log(변수명)으로써 호출해야함 
>블록 외에서 변수명으로 호출 불가